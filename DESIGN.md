---
name: AICodex Admin
product: 身份控制台
framework: React 18 + Ant Design + Vite
colors:
  primary: "var(--ant-color-primary)"
  success: "var(--ant-color-success)"
  warning: "var(--ant-color-warning)"
  danger: "var(--ant-color-error)"
  info: "var(--ant-color-info)"
  bg: "var(--ant-color-bg-container)"
  pageBg: "var(--ant-color-bg-layout)"
  text: "var(--ant-color-text)"
  textSecondary: "var(--ant-color-text-secondary)"
  border: "var(--ant-color-border)"
rounded:
  sm: "4px"
  md: "6px"
  lg: "8px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "16px"
  lg: "24px"
---

# AICodex Admin Design

## 来源与定位

本文档采用 `voltagent/awesome-design-md` 的工作法，但规则来自 AICodex Admin 当前仓库：React 18 + Ant Design + Vite、身份控制台路线、既有后台壳层和本次身份总览试点。

它补充视觉和交互判断，不替代 `web-admin/AGENTS.md` 的工程约束，也不是任务日志。只有稳定、可复用、能指导后续 Admin UI 的规则才写入这里。

## Overview

AICodex Admin 是面向企业管理员的身份控制台。界面目标是管理用户、组织、应用接入、授权、同步和审计证据。设计应强调信息密度、操作确定性、证据可追溯和状态可解释，不做营销式展示。

## Visual Theme And Atmosphere

Ant Design / Ant Design Pro 是主设计基线。IBM Carbon 可作为表格密度、工具栏、列设置和批量操作参考；Fluent 2 可作为焦点态、可访问性和工具型路径参考。不要引入与 AntD 冲突的新视觉系统。

界面保持克制、稳定、可扫描。避免大面积渐变、装饰性插图、过大的 hero、悬浮页面级卡片和不必要的视觉噪音。

页面重点是帮助管理员快速判断“当前身份状态、影响产品域、需要核对的证据位置、下一步动作”。总览页应像后台工作台或数据控制台，不像品牌介绍页。

无法帮助判断或行动的介绍性文案应删除或降级。首屏文案以状态、口径、数量、时间、证据来源和动作标签为主，不写大段价值描述。

## Color Palette And Roles

优先使用 Ant Design CSS token 和仓库既有变量，不在单个页面临时硬编码完整色板。

- `primary`：主操作、当前选中、关键链接。
- `success`：正常、已完成、配置齐备。
- `warning`：待核对、需关注、配置不完整但不阻断。
- `danger`：错误、阻断、危险操作。
- `info`：中性说明、辅助提示、背景状态。
- `bg` / `pageBg`：页面背景与容器背景分层。
- `text` / `textSecondary`：主信息与辅助信息分层。
- `border`：卡片、表格、筛选区和工作台分区边界。

状态色必须少量、稳定、语义一致。不要为单个页面新增高饱和渐变、随机色序列或与 AntD 冲突的色板。

## Typography Rules

- 页面标题应清晰描述业务对象，例如“身份总览”“组织账号”“应用接入”“审计记录”。
- 总览页标题保持工作台尺度，不使用营销页 hero 级大字。
- 表格、筛选、指标摘要优先使用紧凑字号和清晰行高，保证可扫描。
- 指标数值可以突出，但必须配合口径、单位或对象说明。
- 长应用名、provider、组织路径、URL、ID、code tag 必须有换行、截断或 tooltip 策略。

## Layout

- 首屏优先呈现状态摘要、待核对事项、产品域覆盖、证据入口和关键操作。
- 对象详情优先使用页内分区、抽屉、Tabs 或紧凑表单；不要把所有能力堆成入口墙。
- 表格是数据密集页面的主要结构，必须考虑列宽、长文本、固定操作列、批量选择和空态。
- 总览页首屏应同时露出关键指标、主要待核对事项、产品域卡片和至少一组健康/审计证据。
- 列表/报表页优先让筛选和表格可见，不用大标题或说明文字挤压数据区。
- 详情/审计页优先使用页内分区、Tabs、抽屉或紧凑说明面板，不堆叠多层卡片。
- 移动端以单列流式布局为准，表格可局部横向滚动，但页面整体不得横向溢出。

## Component Stylings

- 按钮、表格、表单、弹窗、抽屉、Tabs、Tag、Alert、Tooltip 优先使用 AntD 组件。
- 主动作、次动作、危险动作应分层明确；危险动作必须有确认、影响说明和失败恢复路径。
- 筛选区和工具条使用紧凑布局，保留清晰的查询、重置、刷新、导出或同步动作。
- 指标卡片只用于摘要；不要把所有入口都做成卡片墙。
- 表格是数据密集页面的核心结构，必须考虑列宽、固定操作列、长文本、空态和分页。
- 状态标签语义固定：success 表示正常或完成，warning 表示需核对，danger 表示阻断或错误，processing 表示正在运行，default 表示中性或未知。
- 长 ID、仓库名、回调地址、组织路径、provider 名必须有截断、换行或 tooltip 策略，不得撑破布局。
- 首页或总览页的右侧提醒适合做紧凑状态块，不适合做高饱和 spotlight；提醒文案应直接给出数量和去向。

## Large Edit Page Shell

大型对象编辑页以组织编辑页为当前样板，后续应用、用户、Provider 等编辑页改造时优先复用同一页面壳和交互约定，再根据对象复杂度增减 tabs。

具体迁移步骤、tab 级经验和踩坑记录见 `docs/design/admin-identity-console/large-edit-page-migration-guide.md`。

本节融合公司《系统 UI 设计规范》和 7000 策略编辑页的可落地部分。工程实现仍优先使用 AntD token，不在页面里复制完整色板；当规范给出明确语义时，可映射为局部样式。

- 页面结构：顶部固定对象栏、tabs 导航、中间独立滚动正文、底部固定操作栏。保存、取消、返回不随正文滚动消失。
- 顶部对象栏：返回按钮使用左箭头图标加文字，保持 30px 左右高度；路径信息用 12px 次级文字；对象标题用 14px medium，长文本必须截断或换行。
- Tabs：适合 4 个以上编辑分组或需要横向切换的复杂对象。选中 tab 使用 14px medium 和主文本/主色，未选中 tab 使用 14px 常规深灰蓝；tab 起点与正文左边界保持至少 32px 的桌面端呼吸区。
- 正文密度：桌面端正文内边距优先 20px/24px 垂直、32px 水平；模块间距优先 16px/24px；表单行内间距优先 12px/16px。不要使用营销页尺度的标题和大留白。
- 表单文字：模块标题 14px medium；标签、输入值、选择器值、按钮文字优先 12px；一般说明和表格内容使用 12px 次级正文色。正文和选择框字号不得无意混用 AntD 默认 14px。
- 必填与校验：必填项在标签前使用红色 `*`，保存前做前端校验，并把用户带回第一个错误所在 tab；错误信息使用 12px 错误色。
- 提示信息：正文中的 tip、Alert、Tooltip 不因布局改造丢失。提示应靠近相关配置项，说明影响、前置条件或失败恢复方式。
- 底部操作栏：固定在编辑壳底部，按钮顺序默认“取消 / 保存 / 保存并返回”。主保存为 primary；取消和返回应有脏数据确认；提交中禁用重复提交。
- 可扩展性：先沉淀壳层、tabs、底部操作栏、必填标识、保存前校验等稳定交互；不要把单个业务字段规则抽进公共组件。

## Depth And Elevation

- 使用轻量边框和浅阴影表达容器层级。
- 页面级不要使用漂浮大卡片；卡片只用于明确的数据摘要、工具面板或独立重复项。
- 避免卡片套卡片。需要分组时优先使用分割线、标题栏、Tabs、表格分组或同层网格。
- 弹窗和抽屉用于短流程编辑、确认和详情查看，不承载复杂多页流程。

## Content And States

- 用户可见产品名优先使用“身份控制台”；菜单和页面标题应使用清晰业务标签。
- 不要把内部实现术语、投影细节、provider 原始字段直接作为主导航或主标题。
- 总览页不需要解释产品愿景。能被 breadcrumb、标题、标签、指标表达的信息，不再重复写成长说明。
- 没有真实后端处理流时，不写“待处理”；使用“待核对”“待关注”“核对建议”“核对中”“正常”等状态。
- 所有新增或修改的 UI 必须覆盖 loading、empty、error、permission denied、submitting、success、duplicate submit 和 long text overflow。
- 新增或修改用户可见文案、导航、菜单、按钮、状态、错误信息时，同步维护 zh / en i18n。
- 涉及组织同步、授权、审计、OIDC/OAuth、API 网关映射的页面，应显示口径、范围、时间和证据来源，不伪造处理结果。

## Do And Don't

Do:

- 使用 AntD、Ant Design Pro 和项目既有组件模式。
- 让筛选、指标、表格、审计证据和动作形成连续操作路径。
- 在首屏给出可判断的状态和下一步动作。
- 对长文本、空态、错误态和权限不足做真实处理。

Don't:

- 不做营销式 hero、大面积装饰背景、渐变大标题或品牌站式分屏。
- 不把内部实现词、provider 原始字段、trace 细节作为主标题。
- 不为单个页面新增一套不复用的色彩、圆角、阴影或图表样式。
- 不用嵌套卡片制造层级。
- 不只靠颜色表达状态。

## Responsive Behavior

- 桌面端优先信息密度和横向扫描效率。
- 中等宽度下，筛选区、工具条和状态摘要可以换行，但主动作仍应清楚可见。
- 移动端以单列流式布局为准，复杂表格可局部横向滚动。
- 页面整体不得横向溢出；按钮、标签、应用名、URL、ID 都必须有换行或截断策略。
- 移动窄视口的全局 shell 行为如果是既有问题，不应在单页 UI polish 中顺手大改；需要单独任务处理。

## Accessibility

- 图标按钮必须提供 tooltip、aria label 或相邻文本。
- 自定义样式不得移除焦点态；弹窗、抽屉和表单错误应支持键盘和屏幕阅读器理解。
- 状态不能只靠颜色区分，必须有文字或图标辅助。

## Agent Prompt Guide

AI agent 修改 Admin UI 前应读取本文件、`web-admin/AGENTS.md` 和目标页面实现。若两者冲突，以 `web-admin/AGENTS.md` 的工程约束和当前任务 OpenSpec 为准。

工作方式保持短闭环：

1. 先判断目标页面是否影响状态判断、证据查看、授权/组织/应用接入操作路径。
2. 只做与当前页面直接相关的小范围修改，不顺手重做全局导航、shell、构建配置或接口契约。
3. 优先处理信息密度、无收益文案、表格扫描效率、长文本、空态/错误态和页面级 overflow。
4. UI smoke 必须区分本地 mock、本地前端代理 60 测试后台和真实部署验收，不把截图检查说成后端验收。
