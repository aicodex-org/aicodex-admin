## 1. 数据模型与配置

- [x] 1.1 定义企业微信组织同步配置模型，包含目标组织、Corp ID、自建应用 Secret、启用状态和软禁用策略
- [x] 1.2 定义企业微信组织同步执行记录模型，包含执行人、状态、`created_at`、`updated_at`、`started_at`、`finished_at`、`heartbeat_at`、`lease_expires_at`、统计数量和安全错误摘要
- [x] 1.3 定义企业微信部门映射模型，保存部门 ID、本地 Group owner/name、父部门 owner/name、主负责人缓存和同步状态
- [x] 1.4 定义企业微信成员映射模型，保存 userid、本地 User owner/name、完整 external_id、主部门缓存、状态和同步状态
- [x] 1.5 定义 `wecom_user_department` 关系模型，保存成员部门、本地 User/Group owner/name、主部门标记、部门负责人标记和同步状态
- [x] 1.6 定义 `wecom_department_leader` 关系模型，保存部门完整负责人、本地 Group/User owner/name、主负责人标记和同步状态，并以 `is_primary` 作为主负责人事实来源
- [x] 1.7 定义 `wecom_user_direct_leader` 关系模型，保存成员直属上级、本地 User/Leader owner/name 和同步状态
- [x] 1.8 为 7 张企业微信同步表补充 Xorm 建表注册
- [x] 1.9 为组织、企业 ID、部门 ID、成员 userid、本地 Group/User 映射、3 类关系表、运行状态和最近运行记录补充必要索引、组合唯一约束或事务性 upsert，并为 3 类关系表实现固定长度 `rel-<sha256>` 类对象 `name` 生成规则
- [x] 1.10 确保新增同步表时间字段使用 Go `time.Time` 或可空时间类型，并在 PostgreSQL 下落为 `timestamptz`，接口按 UTC/RFC3339 输出
- [x] 1.11 确保新增同步表布尔字段使用 Go `bool` / PostgreSQL `boolean`，并显式声明 `json` tag 和 `xorm` tag，避免 `IsXxx` 字段名默认推导造成 API 字段漂移
- [x] 1.12 在 PostgreSQL 测试环境验证关系表唯一性、索引查询、软禁用关系恢复，以及 JSON/text 排查字段的读写行为
- [x] 1.13 实现已保存同步配置中敏感 Secret 的脱敏回显和更新保留语义

## 2. 企业微信通讯录客户端

- [x] 2.1 实现企业微信通讯录 token 获取，并处理凭证校验和错误映射
- [x] 2.2 实现企业微信部门快照获取，优先按当前官方推荐接口组合封装，并规范化部门标识、层级、排序和负责人字段
- [x] 2.3 实现企业微信成员快照获取，支持按部门递归拉取或按当前官方推荐接口组合封装，并规范化 userid、资料、联系方式、部门、部门负责人标记、直属上级和状态字段
- [x] 2.4 实现企业微信通讯录连接测试能力，校验凭证、通讯录权限、部门负责人字段和直属上级字段可用性，但不变更本地用户或用户组
- [x] 2.5 在实现前复核企业微信通讯录官方 API 文档，避免把同步服务绑定死在受限或不推荐的单个历史接口上
- [x] 2.6 为企业微信响应规范化和错误处理补充单元测试或聚焦的服务测试

## 3. 全量差异同步服务

- [x] 3.1 实现按组织维度的运行中锁和心跳租约恢复，避免重复触发同步任务并允许清理崩溃遗留 running 记录
- [x] 3.2 实现企业微信部门、成员、部门负责人、直属上级和成员部门关系的完整快照拉取与规范化
- [x] 3.3 实现基于稳定企业微信 ID 的差异计算，禁止清空本地用户、用户组或关系后重建
- [x] 3.4 实现部门 upsert 到 Group，使用包含企业 ID 的稳定 `wecom-dept-<corp_id>-<department_id>` 标识并保存企业微信映射元数据
- [x] 3.5 实现成员 upsert 到 User，以企业微信 userid 作为主外部身份标识，不重命名已绑定用户，并处理 `User.ExternalId` 长度限制
- [x] 3.6 实现企业微信部门成员关系到 `wecom_user_department` 和现有用户组关系的差异同步，只增删企业微信来源部门组并保留非企业微信用户组，同时保证每个用户最多一个启用主部门
- [x] 3.7 保存企业微信部门完整负责人到 `wecom_department_leader`，并让 `Group.Manager` 仅承载兼容展示用的主负责人
- [x] 3.8 保存企业微信成员直属上级关系到 `wecom_user_direct_leader`，并确保该关系不从部门展示层级或成员排序推断
- [x] 3.9 实现已同步但本次完整全量同步缺失的部门和用户的软禁用处理
- [x] 3.10 定义并实现 `running`、`succeeded`、`failed`、`partial` 同步状态和阶段记录
- [x] 3.11 确保同步失败或部分失败时只记录执行结果，不根据缺失数据执行软禁用
- [x] 3.12 持久化成功、失败和部分失败同步执行的统计数量与失败摘要

## 4. 后台接口与权限

- [x] 4.1 增加 `GET /api/wecom-org-sync/config` 配置查询接口，显式解析目标组织并返回脱敏 Secret
- [x] 4.2 增加 `POST /api/wecom-org-sync/config` 配置保存接口，body 中必须包含或能唯一解析目标组织
- [x] 4.3 增加 `POST /api/wecom-org-sync/config/test` 连接和通讯录权限测试接口，按目标组织做权限校验
- [x] 4.4 增加 `POST /api/wecom-org-sync/runs` 手动启动全量差异同步接口，并返回创建的同步执行记录标识或 stale running 恢复结果
- [x] 4.5 增加 `GET /api/wecom-org-sync/runs` 和 `GET /api/wecom-org-sync/runs/:runId` 同步执行记录查询接口，按目标组织过滤并对错误信息做脱敏处理
- [x] 4.6 对 `/api/wecom-org-sync/*` 同步配置和同步执行接口强制执行目标组织管理员权限校验
- [x] 4.7 增加 `GET /api/org-management-scope/current` 当前用户可管理范围接口，覆盖管理员、部门负责人、直属/间接下属和本人范围，并支持多个部门负责人都获得对应管理范围
- [x] 4.8 确保当前用户可管理范围接口只查询当前登录用户范围，不允许普通用户传入任意用户模拟查询
- [x] 4.9 检查并适配 `aicodex-admin` 现有 authz 过滤、审计和前端请求封装，确保模块化路径不被旧 `/api/get-*`、`/api/update-*` 假设遗漏

## 5. Web Admin 界面

- [x] 5.1 新增或扩展 Web Admin 页面，用于配置企业微信组织同步
- [x] 5.2 增加手动同步操作，并覆盖运行中、成功、失败和重复运行状态
- [x] 5.3 增加同步执行历史和详情展示，包含统计数量和安全错误摘要
- [x] 5.4 增加企业微信通讯录权限要求和软禁用策略的校验与帮助文案

## 6. 文档与验证

- [x] 6.1 编写企业微信通讯录权限配置、必需凭证和推荐配置说明
- [x] 6.2 编写部门、用户、成员部门关系、部门负责人关系、直属上级关系和稳定标识的映射规则说明
- [x] 6.3 编写当前用户可管理范围接口契约，明确部门负责人范围与直属/间接下属范围，供后续 `aicodex-insight` 集成使用
- [x] 6.4 文档说明同步状态、失败/部分失败策略、失败不软禁用规则和第一版验收场景
- [x] 6.5 文档说明第一版采用手动全量差异同步，定时全量同步和通讯录回调增量同步属于后续扩展
- [x] 6.6 为同步映射、连接测试、软禁用、失败不软禁用、重复运行拦截、stale running 恢复、ExternalId 超长、非企业微信用户组保留、主部门唯一、多负责人范围计算、直属/间接下属范围计算、关系环去重和管理范围计算补充后端测试
- [x] 6.7 编写 7 张表结构、索引、`Sync2` 建表路径、`timestamptz` 时间字段、布尔字段 tag 约束、关系表查询、PG 测试验证和后续复杂迁移升级策略说明
- [x] 6.8 在测试环境 PostgreSQL 执行启动建表、时间字段类型检查、布尔字段类型和 API 字段名检查、关系表唯一性/重复同步检查、配置保存、连接测试失败、重复运行拦截、stale running 恢复和空管理范围冒烟验证
- [x] 6.9 在标记实现完成前运行 OpenSpec 校验和相关 Go/frontend 检查

## 7. 业务组织绑定与 built-in 防护

- [x] 7.1 同步提案、设计、API、算法和规格文档，明确企业微信同步必须绑定独立业务组织，禁止同步到 `built-in`
- [x] 7.2 基于 Corp ID 实现稳定业务组织名生成和缺失组织初始化，组织显示名支持从企业微信根部门名更新
- [x] 7.3 保存同步配置时，如果从 `built-in` 上下文提交 Corp ID，自动绑定到 Corp ID 对应业务组织并在响应中返回最终组织
- [x] 7.4 手动同步执行前拒绝 `built-in` 目标组织，防止历史配置或绕过前端导致普通员工拥有全局管理员语义
- [x] 7.5 前端保存配置后根据后端返回的最终组织切换上下文，并提示当前同步将写入业务组织
- [x] 7.6 补充后端和前端测试，覆盖组织绑定、`built-in` 拒绝、显示名更新和上下文切换
- [x] 7.7 在测试环境清理误同步到 `built-in` 的企业微信测试数据，重新同步到业务组织并验证用户 owner 不为 `built-in`
- [x] 7.8 初始化或修复业务组织默认应用，并把同步用户绑定到该应用，避免用户编辑缺少应用上下文
- [x] 7.9 头像上传使用同步用户所属应用上下文，避免业务组织用户上传头像时错误查找 `app-built-in` 的 Storage Provider
