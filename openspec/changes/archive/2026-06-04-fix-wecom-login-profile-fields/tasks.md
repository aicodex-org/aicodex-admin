## 1. 企业微信登录字段解析

- [x] 1.0 企业微信扫码登录发起 URL 和内嵌扫码组件保留 Provider 配置的 `scope`
- [x] 1.1 在企业微信内部应用登录用户信息结构中补充 `mobile` 和 `biz_mail` 字段映射
- [x] 1.2 将企业微信 `mobile` 映射为 OAuth 用户信息的 `Phone`
- [x] 1.3 将企业微信 `email` 映射为 OAuth 用户信息的 `Email`，并在 `email` 为空时使用 `biz_mail` 兜底
- [x] 1.4 在授权详情缺少手机号、邮箱、企业邮箱、头像或名称时，使用通讯录详情非空补充

## 2. OAuth 用户资料回填

- [x] 2.1 抽取 OAuth 用户基础资料回填逻辑，统一处理 ID、用户名、显示名、邮箱、手机号和 UnionId
- [x] 2.2 在 Provider 返回手机号且本地手机号为空时补齐 `User.Phone`
- [x] 2.3 保持邮箱和手机号只补空值，不覆盖后台或通讯录同步已经维护的本地资料
- [x] 2.4 保存 `oauth_WeCom_phone` 等 Provider 侧属性，便于后续排查和扩展

## 3. 测试与验证

- [x] 3.1 增加企业微信内部应用登录敏感字段解析测试，覆盖 `mobile` 和 `biz_mail` 兜底
- [x] 3.2 增加 OAuth 回填测试，覆盖空手机号补齐和已有手机号不覆盖
- [x] 3.3 增加授权详情缺字段时调用通讯录详情补齐的回归测试
- [x] 3.4 增加企业微信扫码登录 URL 和内嵌扫码组件携带 `scope` 的前端回归测试
- [x] 3.5 运行企业微信 IdP 包测试、OAuth 回填定向测试和企业微信登录前端定向测试
- [x] 3.6 执行 diff 空白检查，确认没有格式噪声
- [x] 3.7 完成企业微信客户端 OAuth2 敏感授权运行态验证，确认 PC 扫码 SSO 与敏感资料授权入口的边界
