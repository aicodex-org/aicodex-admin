## 1. 同步记录展示优化

- [x] 1.1 调整 `web-admin/src/WecomOrganizationSyncPage.js` 的同步记录表头与单元格展示，让部门和用户统计在表格内部即可表达“新增 / 更新 / 禁用”语义
- [x] 1.2 移除或替换表格外独立的统计顺序提示，确保管理员无需依赖悬空说明即可理解每行统计值
- [x] 1.3 补充页级加载态和状态列中文文案，避免账号或配置加载阶段出现空白首屏，并保持同步记录区域文案一致性

## 2. 运行中刷新机制

- [x] 2.1 为企业微信同步页增加运行中自动轮询逻辑，仅在当前组织存在 `running` 记录时按固定间隔刷新同步记录
- [x] 2.2 在同步记录区域增加显式手动刷新入口，并处理切换组织、页面卸载、终态停止和刷新失败后的清理逻辑
- [x] 2.3 在同步记录区域增加自动刷新状态提示、最近刷新时间和手动刷新 loading 反馈，让管理员能感知页面是否仍在持续观察运行中任务
- [x] 2.4 当页面已识别到 `running` 记录时，收紧“开始全量同步”入口；若后端仍返回 `already running` 竞态冲突，前端以提示并刷新同步记录代替失败 toast
- [x] 2.5 修复同步记录表格分页导航，点击页码后按所选 `page/pageSize` 重新请求历史页数据，并在历史页给出“返回第 1 页查看最新状态”的提示
- [x] 2.6 抽取共享表格分页 helper，统一 BaseListPage 派生页和独立列表页的分页默认配置，避免重复散落的 `showQuickJumper/showSizeChanger/showTotal` 实现

## 3. 测试与校验

- [x] 3.1 更新 `web-admin/src/WecomOrganizationSyncPage.test.js`，覆盖统计自解释展示、自动轮询触发/停止和手动刷新行为
- [x] 3.2 运行前端相关测试与 `openspec validate "improve-wecom-sync-run-monitoring" --strict`，确认 change 工件和实现入口可用
- [x] 3.3 通过 local dev + MCP 浏览器重新验证加载态、刷新状态提示和手动刷新反馈在真实页面可见
- [x] 3.4 补充“运行中禁用启动入口”和“后端返回 `already running` 时提示并刷新”的前端测试与验证记录
- [x] 3.5 补充同步记录历史页翻页测试与共享分页 helper 测试，并通过前端构建验证批量分页收口后的语法与 lint 正常
