## 1. 已完成范围：IA 审计与导航重构方向

- [x] 1.1 审计当前 `enterpriseNavigation` 与运行态侧栏入口，并按主业务域、上下文能力、流程入口和遗留路由分类。
- [x] 1.2 定义目标主 IA：中心总览、组织与账号、应用接入、身份源、权限与角色、审计运维和 LLM AI/Gateway。
- [x] 1.3 将身份资产关系、接入预检、治理任务、快捷操作等抽象治理能力默认降级到总览链接、对象详情动作或流程入口，同时保留路由兼容。
- [x] 1.4 更新 `zh` / `en` 导航文案和导航测试，使标签使用管理员可理解的业务语言，而不是实现术语。
- [x] 1.5 纳入主控补充 IA/文案门禁：一级菜单和菜单文档命名默认四字中文业务名优先，专有技术词允许中英混合，本次不改实现或 locale。

## 2. 归档前收口

- [x] 2.1 将本 umbrella scope 收窄为“已完成 IA/导航方向门禁”，不继续声明视觉系统、对象上下文迁移或浏览器证据已经完成。
- [x] 2.2 在 `design.md` 中记录未完成 UI/视觉/对象上下文任务的 `DEFER` 清单，作为未来独立 change 候选。
- [x] 2.3 裁剪 delta spec，只把 IA 降级和后续 UI 任务门禁同步到主规格。
- [x] 2.4 补充归档前验证记录，说明本轮为 OpenSpec/docs-only 收口，覆盖率、typecheck、build 和浏览器验证为 N/A。

## Deferred / 从本 umbrella 裁出的未来候选

- `DEFER`: 紧凑企业页头、专业表格工具栏、状态标签和空/错/权限态视觉系统实现。
- `DEFER`: 替换代表性页面的大工作台/卡片堆叠，让核心列表或核心操作回到首屏。
- `DEFER`: 将关系、证据、预检、治理任务和快捷操作迁移到对象详情、行操作、配置流程或结果页。
- `DEFER`: 使用 Playwright/local-dev/60 证据验证桌面 `1440x900`、移动 `390x844`、表格首屏坐标、无旧 Tour/overlay、无横向溢出和无 console warning/error 回归。
