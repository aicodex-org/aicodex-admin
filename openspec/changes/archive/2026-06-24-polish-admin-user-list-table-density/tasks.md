## 1. OpenSpec 与设计收口

- [x] 1.1 创建用户列表密度 polish 的 proposal、design、delta spec 和 tasks。
- [x] 1.2 完成实施前 review，确保范围只覆盖用户列表页和必要共享样式/i18n。

## 2. 前端实现

- [x] 2.1 将用户列表接入共享查询工具栏和共享列表页表格壳，保持现有查询、排序、分页参数兼容。
- [x] 2.2 收敛用户列表默认列，合并用户识别、联系方式和来源归属信息，隐藏低频详情字段。
- [x] 2.3 为头像添加失败兜底，并将验证等只读状态改为标签化展示。
- [x] 2.4 降低行操作按钮噪声，保留模拟登录、编辑、删除和移出群组的既有语义。
- [x] 2.5 补充必要 zh/en locale 和页面样式，桌面端默认消除横向滚动轴。
- [x] 2.6 将用户页接入共享查询工具栏的“更多筛选”插槽，更多筛选字段使用英文冒号并映射到现有单字段后端查询契约。
- [x] 2.7 为组织、群组、用户列表补齐共享 `enterprise-list-*` 主文本、辅助文本、链接、状态标签和行操作 class，统一列表字号语义。

## 3. 验证

- [x] 3.1 更新 `UserListPage.test.tsx` 覆盖默认列、工具栏查询、头像兜底、状态标签和行操作语义。
- [x] 3.2 运行 `openspec validate polish-admin-user-list-table-density --strict`、`git diff --check`、聚焦 Jest、覆盖率、TypeScript gate/typecheck/build。
- [x] 3.3 启动本地预览并用浏览器截图或 DOM 证据验证 `/users` 首屏表格密度、无默认横向滚动、操作可达。
- [x] 3.4 更新 `verification.md` 记录命令、结果、截图/预览 URL 和剩余风险。
- [x] 3.5 补充并运行用户、群组、组织列表聚焦 Jest，覆盖用户页更多筛选和跨列表共享字号 class。
