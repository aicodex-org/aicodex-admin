## 1. 基线审计

- [x] 1.1 审查组织、群组、用户和应用接入列表的标题、动作、上下文、分页实现差异，确认需要进入公共列表页壳的属性和样式。
- [x] 1.2 审查现有共享组件测试，确定本次新增或调整的聚焦测试范围。

## 2. 测试先行

- [x] 2.1 为组织账号列表补充失败测试，覆盖标题来源、动作槽、组织目录健康上下文槽和分页公共配置。
- [x] 2.2 为应用接入列表补充失败测试，覆盖标题来源、动作槽、共享表格壳和分页公共配置。
- [x] 2.3 为共享工具栏或分页组件补充失败测试，锁定右侧动作、辅助上下文和分页导航的一致性契约。

## 3. 公共列表页壳实现

- [x] 3.1 调整共享列表工具栏、表格或分页组件，使标题、动作、辅助上下文和分页区域使用同一套 slot/class/布局属性。
- [x] 3.2 将组织、群组、用户页面接入共享列表页壳，移除会造成标题或动作漂移的页面私有 top action 用法。
- [x] 3.3 将资源、证书、密钥、Webhook 回调和 Webhook 事件页面对齐共享列表页壳，保留各自业务按钮和筛选字段差异。
- [x] 3.4 统一相关样式 token/class，确保左侧标题、右侧功能入口、辅助上下文和分页导航可通过公共属性继续调整。

## 4. 验证与文档

- [x] 4.1 运行聚焦前端测试、`yarn typecheck`、OpenSpec strict 校验和必要的 diff 检查。
- [x] 4.2 启动本地前端并连接 60 测试后台，对目标列表页面做只读浏览器验证，不点击写操作。
- [x] 4.3 记录验证结果和剩余风险到 `verification.md`，并更新任务状态。

## 5. 反馈迭代：接入中心与身份源中心

- [x] 5.1 将 `/applications` 和 `/providers` 纳入共享列表页壳一致性测试，覆盖标题、动作和分页位于同一 shell。
- [x] 5.2 为资源、证书、密钥、Webhook 回调和 Webhook 事件列表补齐 `.enterprise-list-page-table-shell`，其中 Webhook 事件保留无主动作语义。
- [x] 5.3 为身份源中心 Provider 列表补齐共享 shell class，保留 Provider 既有新增、编辑、删除、搜索、排序和分页契约。
- [x] 5.4 将组织/用户 compact 无 Card 路由的外层间距、边框和卡片样式收敛到共享 `--list-page-*` token。
- [x] 5.5 重新运行聚焦测试，并补充本轮规格和验证记录。

## 6. 反馈迭代：组织上下文与添加按钮样式

- [x] 6.1 将组织页目录健康上下文从标题动作同一行移到共享工具栏标题下方独立行，避免挤占“添加”入口。
- [x] 6.2 去掉证书、密钥和 Webhook 回调常规“添加”按钮的页面私有 `PlusOutlined` 图标，统一到群组/组织/用户的文字按钮样式。
- [x] 6.3 补充聚焦测试，锁定标题下方上下文槽和应用接入常规新增按钮无 plus icon 的一致性契约。

## 7. 反馈迭代：右侧主动作统一壳

- [x] 7.1 为共享工具栏补充测试，锁定无辅助上下文时也能通过公共属性把主动作放到标题区右上。
- [x] 7.2 将群组、用户、应用、身份源 Provider、资源、证书、密钥和 Webhook 回调列表接入同一个右上主动作布局属性。
- [x] 7.3 运行聚焦测试、类型检查和只读浏览器验证，确认其它列表页与组织页保持同一套壳。
