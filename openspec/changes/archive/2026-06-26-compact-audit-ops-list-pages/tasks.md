## 1. OpenSpec 与设计收口

- [x] 1.1 创建审计运维列表紧凑化和统一列表治理壳的 proposal/design/spec/tasks。
- [x] 1.2 运行实施前 OpenSpec 校验，确认 change 可以进入实现。

## 2. 测试先行

- [x] 2.1 为会话、操作日志、令牌、验证列表增加“不渲染审计运维摘要块”的回归测试。
- [x] 2.2 为会话、操作日志、令牌、验证列表增加统一列表壳、搜索/更多筛选、按钮和行操作测试。
- [x] 2.3 为令牌授权码、访问令牌和验证码不进入列表列增加回归测试。

## 3. 前端实现

- [x] 3.1 从四个分页列表移除审计运维摘要块，让列表标题和搜索栏直接进入首屏。
- [x] 3.2 将四个旧列表页接入 `ListPageTable`、`LegacyListPageToolbar` 和 `ListPageRowActions`。
- [x] 3.3 移除四个列表页表头默认列搜索入口，保留排序、分页和原有业务动作。
- [x] 3.4 添加审计运维列表局部样式，避免修改公共列表壳全局变量。
- [x] 3.5 收敛四个表格列宽，移除审计运维页面的 `max-content` 横向滚动策略。
- [x] 3.6 为四个表格配置桌面 `scroll.y`，对齐组织等页面的固定表头和表体滚动体验。
- [x] 3.7 优化操作日志详情抽屉，增加内部滚动容器、摘要层级和限高代码区。
- [x] 3.8 精简操作日志详情抽屉卡片感，让对象载荷跟随主滚动，并为脱敏代码区增加复制按钮。
- [x] 3.9 过滤 `Editor` 内部布局和主题 props，避免详情抽屉打开时产生 React DOM 属性警告。
- [x] 3.10 将会话列表标题和导航语义收敛为“登录会话”，将 `name` 列显示为“用户”，并补充应用列。
- [x] 3.11 折叠登录会话列表大量 Session ID，提供全部 Session ID 抽屉，并明确单个会话踢出与应用下全部会话删除的操作文案。

## 4. 验证

- [x] 4.1 运行增量 TypeScript 门禁、聚焦 Jest 和覆盖率检查。
- [x] 4.2 运行 `yarn typecheck` 和必要构建检查。
- [x] 4.3 启动本地前端并连接 60 后台，使用浏览器验证四个页面首屏、搜索、更多筛选、按钮、分页、横向滚动和固定表头。
- [x] 4.4 更新 `verification.md`，记录命令、结果、覆盖率、浏览器验证和剩余风险。
- [x] 4.5 使用浏览器验证操作日志详情抽屉可以滚动到底，技术详情和脱敏内容默认可见。
- [x] 4.6 验证操作日志详情复制按钮只复制脱敏展示值，且对象载荷不再使用固定小高度内置滚动。
- [x] 4.7 使用浏览器复验详情抽屉打开后不再产生 `Editor` 内部 props 透传警告。
