## 1. OpenSpec 对齐

- [x] 1.1 补齐角色/权限列表优先和取消顶部工作台的 proposal、design 与 delta specs。
- [x] 1.2 补齐权限和 Casbin 列表取消不必要固定列的 delta spec。

## 2. 前端实现

- [x] 2.1 调整 `OrganizationIdentityCenter`，让角色和权限页直接呈现紧凑统一列表壳。
- [x] 2.2 移除角色、权限、模型、适配器和执行器表格中不必要的固定列配置。
- [x] 2.3 清理已废弃的角色/权限工作台样式，保持列表页间距紧凑。

## 3. 测试与验证

- [x] 3.1 更新角色/权限、模型/适配器/执行器和身份中心 wrapper 的聚焦测试。
- [x] 3.2 运行 OpenSpec 校验、聚焦 Jest、TypeScript 检查和前端构建。
- [x] 3.3 使用本地预览连接 60环境做浏览器检查，确认目标页面不存在固定列 sticky 单元格。
