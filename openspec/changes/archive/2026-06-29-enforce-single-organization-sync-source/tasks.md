## 1. 后端同步源状态与互斥

- [x] 1.1 为企业微信和飞书同步配置服务增加跨 Provider 状态查询能力，返回默认组织、已配置状态和另一 Provider 是否已占用。
- [x] 1.2 为保存企业微信配置增加飞书配置占用冲突测试，并实现保存层互斥校验。
- [x] 1.3 为保存飞书配置增加企业微信配置占用冲突测试，并实现保存层互斥校验。
- [x] 1.4 为企业微信和飞书手动同步启动增加冲突测试，并在创建 run 前拒绝冲突组织。
- [x] 1.5 收紧保存层互斥：同组织已有另一 Provider 配置时，未启用草稿保存也必须被拒绝。
- [x] 1.6 将互斥语义从另一 Provider 已启用收紧为另一通讯录来源已配置/已占用，并返回占用组织列表。

## 2. 调度兜底

- [x] 2.1 为企业微信定时同步派发增加另一 Provider 已配置时跳过/失败的测试和实现。
- [x] 2.2 为飞书定时同步派发增加另一 Provider 已配置时跳过/失败的测试和实现。

## 3. 前端同步组织恢复与冲突提示

- [x] 3.1 扩展企业微信和飞书前端 backend API 类型，读取后端默认组织和冲突摘要。
- [x] 3.2 为企业微信同步页增加进入页面自动恢复已配置/最近选择组织的测试和实现。
- [x] 3.3 为飞书同步页增加进入页面自动恢复已配置/最近选择组织的测试和实现。
- [x] 3.4 为两页增加另一 Provider 已配置/占用时的 warning、启用开关禁用和正式同步禁用测试与实现。
- [x] 3.5 收紧两页冲突态体验：同组织冲突时禁用保存按钮，并更新 warning 文案说明只读查看。
- [x] 3.6 为连接旧后端的本地前端补充对侧同步配置兜底检测，避免后端未返回冲突摘要时漏提示。
- [x] 3.7 为两页组织下拉过滤已被其他通讯录来源占用的组织，并保留当前冲突组织显示用于解释只读状态。

## 4. 验证与规格同步

- [x] 4.1 运行后端聚焦测试、前端聚焦 Jest、`yarn typecheck`、OpenSpec strict 校验和 `git diff --check`。
- [x] 4.2 将变更同步到主规格或保留 active change 状态，并记录剩余风险。
- [x] 4.3 追加占用语义和下拉过滤后，重新运行后端聚焦测试、前端聚焦 Jest、`yarn typecheck`、OpenSpec strict 校验和 `git diff --check`。

验证记录：本轮保持 `enforce-single-organization-sync-source` 为 active change，主规格留待归档时合入。剩余风险是尚未在 60/69 运行态环境做真实企业微信/飞书配置页面联调和定时调度 smoke。
