# Tasks

- [x] 1. 为组织、用户、应用、Provider、Syncer 主编辑 Card 增加统一 `admin-large-edit-card` 边界，并补齐缺失页面 hook。
- [x] 2. 在 `App.less` 中用 scoped 规则统一主表单 label/content 列伸缩和窄屏换行。
- [x] 3. 增加或更新聚焦测试，约束大编辑页 class hook 与布局样式契约。
- [x] 4. 运行 OpenSpec、聚焦 Jest、增量 TS gate、typecheck，并记录验证结果。
- [x] 5. 可行时运行浏览器 smoke，检查核心大编辑页没有页面级横向溢出和双层壳回退。
