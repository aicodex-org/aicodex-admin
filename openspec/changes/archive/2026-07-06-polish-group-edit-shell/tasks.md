## 1. OpenSpec 与实施边界

- [x] 1.1 创建并校验 `proposal.md`、`design.md`、delta specs 和 `tasks.md`。
- [x] 1.2 确认群组编辑页迁移范围不包含群组成员管理页、用户列表移出群组或新增后端 API。

## 2. 群组编辑页结构迁移

- [x] 2.1 在 `GroupEditPage.tsx` 增加 dirty、validation 和 submitting 状态。
- [x] 2.2 将旧 Card title 保存按钮和正文底部重复按钮迁移为顶部返回路径、基础信息区块和底部固定操作栏。
- [x] 2.3 保留组织、名称、显示名称、类型、上级组、启用状态的字段 handler 和保存 payload 语义。
- [x] 2.4 将 `用户` 呈现为只读当前成员摘要，并保留目录同步只读提示。
- [x] 2.5 保留新增模式取消删除临时群组和 group tree URL 返回语义。
- [x] 2.6 将目录同步群组的同步标识、组织、类型、上级组和成员关系明确为来源系统托管字段，并保留显示名称、启用状态本地编辑。
- [x] 2.7 为多人群组成员摘要增加折叠展示，避免成员标签撑开页面。
- [x] 2.8 为非目录同步群组成员摘要提供既有群组树/用户列表上下文跳转入口，目录同步群组不提供本地成员维护入口。

## 3. 校验、i18n 与样式

- [x] 3.1 为群组 `名称`、`显示名称` 增加必填红星和保存前校验。
- [x] 3.2 为新增标题、路径、未保存状态、确认、校验和成员摘要文案补充 zh/en locale。
- [x] 3.3 在 `App.less` 增加群组编辑页 scoped 样式，复用组织编辑页密度和暗色主题 token。
- [x] 3.4 将非目录同步群组 `名称` 文案调整为 `群组标识`，并让工作区标签使用群组标识/显示名称区分多开页面。

## 4. 测试与验证

- [x] 4.1 更新 `GroupEditPage.test.tsx` 覆盖单页编辑壳、固定操作栏、必填校验、dirty 取消/返回确认、保存/保存并返回和新增取消清理。
- [x] 4.2 更新测试覆盖只读成员摘要、无成员空态和目录同步提示。
- [x] 4.3 更新测试覆盖目录同步来源托管字段禁用、同步标识标签、成员摘要折叠和成员 profile 查询失败回退。
- [x] 4.4 更新测试覆盖成员维护跳转入口、群组详情工作区标签兜底标题和显示名动态更新。
- [x] 4.5 运行 OpenSpec strict validate、`git diff --check`、incremental TypeScript gate、`yarn typecheck` 和聚焦 Jest。
- [x] 4.6 如本地预览可用，使用前端代理 60 测试后台检查群组编辑页浅色/暗色视觉、无横向溢出和无新增 console/page error。（7004 前端代理可用；Playwright 新上下文访问 60 目标路由被登录页阻断，已补充 mock API 浏览器 smoke 验证浅色/暗色群组编辑页、成员入口、工作区标签和横向 overflow 指标。）
