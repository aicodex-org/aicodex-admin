## 1. 导航数据与壳层结构整理

- [x] 1.1 梳理 `ManagementPage.js` 现有菜单分组、叶子路由和 `navItems`/`userNavItems` 过滤逻辑，抽取可复用的导航配置生成函数
- [x] 1.2 保留现有 `selectedMenuKey` 映射能力，确保深链接路由仍能定位到正确的导航分组和叶子菜单
- [x] 1.3 明确“认证中心”作为顶部唯一业务入口的渲染方式，并为桌面端侧栏与移动端抽屉共用同一份菜单数据

## 2. 后台布局重构

- [x] 2.1 将桌面端后台布局从顶部横向菜单改为“顶部工具栏 + 左侧认证中心导航 + 内容区”结构
- [x] 2.2 将原红框区域中的首页、用户管理、身份认证、Casbin 权限管理、LLM AI、日志与审计、商业与付款、管理工具迁移到左侧导航中
- [x] 2.3 保留顶部工具区、账号区、组织切换、主题切换和语言切换能力，不让侧栏改造影响这些现有控件
- [x] 2.4 复用相同导航配置更新移动端抽屉菜单，确保小屏下仍可从左侧打开认证中心菜单

## 3. 品牌替换

- [x] 3.1 将 `web-admin` 主壳层中的 旧项目标题、页脚品牌文案和默认描述替换为 aicodex-admin
- [x] 3.2 调整默认 Logo 资源和相关配置入口，保证浅色/深色主题下都能展示 aicodex-admin 品牌
- [x] 3.3 在正式 logo 资产未就绪时提供 aicodex-admin 文本锁定或配置化图片回退，避免品牌改造被资源阻塞
- [x] 3.4 保留组织级自定义 Logo 覆盖能力，避免品牌默认值破坏已有组织配置

## 4. 默认中文策略

- [x] 4.1 将前端默认语言配置调整为中文，并梳理 `i18n.js` 的初始化优先级
- [x] 4.2 移除或收敛服务端 `forceLanguage`、浏览器语言、账号语言对缺省语言的静默覆盖，确保无显式偏好时稳定落在中文
- [x] 4.3 保留 URL `language` 参数、手动语言切换和本地持久化优先级，避免影响已有显式语言选择

## 5. 回归验证

- [ ] 5.1 验证管理员和非管理员账号下的左侧导航权限过滤结果符合当前 `navItems` / `userNavItems` 规则
- [ ] 5.2 验证桌面端和移动端的导航展开、选中态、高亮和页面跳转行为一致
- [ ] 5.3 验证首次访问默认中文、手动切换语言持久化、以及品牌文案替换后的页面标题与页脚展示
- [x] 5.4 运行 `web-admin` 构建或等效校验，确认壳层重构未引入编译错误
