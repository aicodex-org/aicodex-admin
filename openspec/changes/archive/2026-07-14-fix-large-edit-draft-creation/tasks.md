## 1. 提案与测试基线

- [x] 1.1 完成实施前 review，核对七个统一编辑壳对象及应用复制入口的当前读写与路由边界。
- [x] 1.2 为应用、组织、用户、群组、角色、权限和 Provider 的列表新增入口编写 RED 测试，证明点击新增不调用新增 API、不显示新增成功并携带路由草稿。
- [x] 1.3 为七个编辑页编写 RED 测试，覆盖新增草稿加载和跳过详情读取、保存新增、保存后更新、取消/返回/放弃脏草稿不调用 mutation。

## 2. 前端草稿实现

- [x] 2.1 改造应用列表及编辑页：新增和复制仅传递草稿，新增保存创建、取消/返回不删除。
- [x] 2.2 改造组织、用户和群组列表及编辑页：新增仅传递草稿，新增保存创建、取消/返回不删除。
- [x] 2.3 改造角色、权限和 Provider 列表及编辑页：新增仅传递草稿，新增保存创建、取消/返回不删除。
- [x] 2.4 运行各对象聚焦 Jest，确认 RED 测试转绿且既有编辑、校验、返回和删除行操作回归通过。
- [x] 2.5 修复群组树根/子群组和三个组织同步页目标组织的替代新增入口，统一为零 mutation 路由草稿。
- [x] 2.6 修复用户新增/编辑页初始及切换组织上下文的乱序、失败和卸载保护，加载未完成时保存 fail-closed。

## 3. 静态与受控运行态验证

- [x] 3.1 运行受影响测试的 coverage，记录受影响实现文件的覆盖率和未覆盖原因（如有）。
- [x] 3.2 运行增量 TypeScript gate、`yarn typecheck`、`yarn build`、`git diff --check` 与 OpenSpec strict validate。
- [x] 3.3 启动本地前端代理 60，使用拒绝所有非 GET 的受控浏览器逐页验证应用、组织、用户、群组、角色、权限和认证源入口的新增、取消与顶部返回；保存按钮不执行真实请求。
- [x] 3.4 将测试、构建、浏览器 smoke、非 GET 拦截和未触发真实写入的脱敏证据写入 `verification.md`。
- [x] 3.5 将群组树、三个组织同步入口和用户组织切换纳入受控浏览器 smoke，并更新脱敏验证记录。

## 4. 归档前审查

- [x] 4.1 执行归档前 review，修复代码、测试、spec、验证记录或文档中的明确阻塞项。
- [x] 4.2 确认 change archive-ready，并按收口流程压缩为一个 change commit、推送工作分支；未经用户明确授权不合入共享分支。
