## 1. 路由标签元数据

- [x] 1.1 为 workspace navigation route metadata 增加编辑标题和详情 path depth，并为已有对象编辑路由集中声明准确深度。
- [x] 1.2 让 workspace tab 状态仅对精确详情深度派生编辑标题，保留列表页、群组详情和动作子路由的既有语义。
- [x] 1.3 区分路由默认标题和详情标题来源，避免普通路由切换覆盖已加载的对象显示名称。
- [x] 1.4 验证列表 route 复用、对象编辑和新增草稿按完整 path 独立，以及动作子路由不创建对象编辑标签的状态不变量。

## 2. 业务显示名称更新

- [x] 2.1 复用现有 workspace tab 标题更新事件，使应用编辑页加载详情或修改显示名称后更新当前标签。
- [x] 2.2 复用同一事件，使组织编辑页加载详情或修改显示名称后更新当前标签。
- [x] 2.3 复用同一事件，使证书编辑页加载详情或修改显示名称后更新当前标签。
- [x] 2.4 让证书编辑页的页内标题与工作页标签统一使用显示名称优先规则。
- [x] 2.5 分批接入低风险对象编辑页：AI Gateway（Agent、Enforcer、Entry、Server、Site）、商业付款（Order、Payment、Plan、Pricing、Product、Subscription）及 Ticket。

## 3. 测试与验证

- [x] 3.1 按 TDD 补充 route metadata、对象编辑标题、动作子路由保护和应用显示名称更新的聚焦测试。
- [x] 3.4 覆盖详情标题更新后切换列表及其它编辑页时的状态保持行为。
- [x] 3.5 覆盖组织编辑页加载和修改显示名称后的工作页标题更新。
- [x] 3.6 覆盖同一列表、两个不同对象路径及重复打开同一路径时的标签去重与并存行为。
- [x] 3.7 覆盖证书编辑页加载和修改显示名称后的工作页标题更新。
- [x] 3.8 覆盖证书页内标题使用显示名称且在为空时回退技术标识。
- [x] 3.9 为低风险对象编辑页补充详情加载和显示名称更新的聚焦测试，并验证不读取关联对象显示名称。
- [x] 3.2 已运行 workspace tabs、导航和应用编辑页聚焦测试及受影响文件 coverage；`ApplicationEditPage.tsx` 为 48.41%，未达到 85%，用户已于 2026-07-15 明确接受本 change 的例外处置，后续由 `improve-application-edit-testability` 独立 change 治理。
- [x] 3.3 运行 typecheck、增量 TypeScript gate、OpenSpec strict validation，并使用本地前端代理测试后台做浏览器验证。
